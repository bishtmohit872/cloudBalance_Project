const Bullet=({number})=>{
    return(
        <div className="size-6 rounded-full bg-[#889ABE] text-white flex items-center justify-center mr-2">
            <p>{number}</p>
        </div>
    )
}

export default Bullet